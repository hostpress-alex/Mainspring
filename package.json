{
    "name":            "projectmanager",
    "private":         true,
    "description":     "Convenience wrapper. The real projects are backend/ and frontend/.",
    "scripts":         {
        "predev": "npm run stop --silent",
        "dev":    "concurrently -n api,web -c blue,magenta --kill-others \"npm --prefix backend start\" \"npm --prefix frontend start\"",
        "stop":   "sh scripts/stop-ports.sh",
        "build":  "npm --prefix frontend run build",
        "test":   "npm --prefix backend test && npm --prefix frontend test -- --run",
        "setup":  "npm --prefix backend install && npm --prefix frontend install"
    },
    "devDependencies": {
        "concurrently": "^10.0.5"
    }
}
