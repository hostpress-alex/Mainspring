/**
 * The name of the application, in exactly one place.
 *
 * It used to sit twice: here as a constant and again as a literal in the
 * <title> of index.html — which is the copy everybody forgets. index.jsx sets
 * the document title from this value, so renaming the app is this line.
 */
export const APP_NAME = 'Mainspring'

/** Shown in the browser tab and used by the manifest. */
export const APP_TITLE = APP_NAME
