# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js
package-lock.json
/backend/package-lock.json
/frontend/package-lock.json
/.idea

# testing
/coverage
*/_to_delete
**/_to_delete
*/node_modules
*/logs
*/uploads/*

# production
/build
frontend/build

# The built frontend that the server hands out in production. Build output,
# not source: it is stale the moment it is written, and the old react-scripts
# version sat here at 19.6 MB, 17 of them a sourcemap for code that no longer
# exists. Produced by "npm run build" in the frontend.
backend/public

# misc
# Zugangsdaten zur Datenbank gehoeren nicht ins Repository
.env
backend/.env
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

todo.txt
HANDOVER.md
CLAUDE.md

.DS_Store